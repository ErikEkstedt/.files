# /home/erik/.config/tmuxinator/WORK.yml
name: WORK
root: ~/voicegen
windows:
    - VIM:
        layout: main-vertical
        panes:
          - Editor:
            - vim
          - Repl:
            - cd voicegen
            - source activate project0
            - ipy
            - import numpy as np
          - Script:
            - source activate project0
    - BLOG:
        root: ~/blog/public/notes
        panes:
          - vim ./ML/voice-generation.md
    - PHD:
        root: ~/phd
    - CONFIG:
        root: ~/.files
