name: TurnTaking
root: ~/Dialog/turntaking
windows:
    - TurnTaking:
        layout: main-vertical
        panes:
          - Editor:
            - tree -L 2
          - Repl:
            - so conversation
            - ipy
