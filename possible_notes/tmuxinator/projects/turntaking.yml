name: TurnTaking
root: ~/Dialog/turntaking
windows:
    - TurnTaking:
        layout: main-vertical
        panes:
          - Editor:
            - tree -L 1
          - Repl:
            - so tt
            - ipy
